import BasicCards from '../Fragments/basicCards';

const AnimalCards = () => {
  return (
    <section className="w-full bg-slate-600 drop-shadow mt-5 p-5 ">
      <BasicCards />
    </section>
  );
};

export default AnimalCards;
